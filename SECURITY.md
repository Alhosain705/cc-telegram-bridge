# الأمان

## نموذج الخطر بلغة بسيطة

البوت ليس مجرد دردشة. هو باب يوصل Claude Code بجهازك، وClaude Code يقدر يقرأ ملفات وينفّذ أوامر بحسب الصلاحيات اللي تعطيه.

إذا انسرق رمز البوت، أو سمحت لشخص غلط، أو اخترت «أشتغل بحرية» على جهازك الأساسي، ممكن تتضرر ملفاتك وخصوصيتك.

## الإعداد الآمن

- استخدم جهازاً مخصصاً أو جهازاً قديماً منظّفاً من بياناتك.
- لا تضع ملفات شخصية أو كلمات مرور أو مفاتيح مهمة في مجلد عمل Claude.
- لا ترسل رمز البوت أو رمز الربط لأي شخص.
- اربط حساب المالك الوحيد فقط؛ الإصدار الأول لا يدعم مستخدمين إضافيين أو مجموعات.
- ابدأ بوضع «أوافق على كل خطوة»، واعتمد الطلب الحساس من زر **✅ موافق** المرتبط به فقط. كتابة «موافق» في رسالة عادية لا تعتمد شيئاً.
- لا تفعّل «أشتغل بحرية» إلا على جهاز مخصص خالٍ من بياناتك الشخصية، وأنت فاهم أن Claude يقدر ينفّذ بدون سؤال.
- لا تترك `ANTHROPIC_API_KEY` في بيئة ويندوز إذا كنت تبي استخدام حصة الاشتراك.

## وش يحميه البرنامج؟

- يرفض التشغيل إذا لم يوجد مالك رقمي واحد بالضبط.
- يرفض قيم المثال قبل التشغيل.
- يولّد رمز ربط عشوائياً من 6 أرقام، بصلاحية 10 دقائق، وعدّ محاولات دائم، ويبطله عند أول نجاح.
- يفحص الرسائل والأزرار بنفس بوابة التصريح.
- في الوضع الآمن يستخدم قناة الصلاحيات المهيكلة الرسمية في Claude Code. كل طلب حساس يرتبط ذرياً بالمهمة والمحادثة والمالك ومعرّف طلب الأداة نفسه، ويُعرض بزرين لمرة واحدة فقط.
- زر الموافقة يحمل معرّفاً عشوائياً قصيراً والقرار فقط؛ لا يحمل أمراً أو مساراً أو سراً. تفاصيل الطلب الكاملة تبقى في الذاكرة ولا تُحفظ في `state.json`، والوصف المعروض منقّى. إذا تعذّر عرض الوصف كاملاً ضمن الحد الآمن، يُرفض الطلب قبل إرساله إلى تيليجرام ويُطلب من Claude تقسيمه.
- طلب الموافقة ينتهي بعد 10 دقائق بالرفض. الرفض، وتكرار الضغط، والمالك أو المحادثة الخطأ، وإيقاف المهمة، وإغلاق الجسر أو فقد الوسيط كلها تفشل مغلقة ولا تتحول إلى سماح.
- الوضع الآمن يرفض بدء المهمة إذا كان سياق وسيط الموافقة ناقصاً، ويمرّر `--strict-mcp-config` حتى لا تدخل خوادم MCP من إعداد المستخدم أو المشروع خارج الوسيط المحدد للجسر.
- يبني بيئة Claude الابن من قائمة سماح صغيرة، فلا تنتقل متغيرات AWS أو Azure أو GCP أو `*_FILE` أو الأسماء غير القياسية.
- لا يمرر اعتماد بوابة `ANTHROPIC_AUTH_TOKEN` بلا وجهته، ولا يدعم بوابات/proxy/CA المؤسسية في الإصدار الأول. فحص تسجيل الدخول في اللانشر يستخدم قائمة البيئة نفسها.
- يحجب `ANTHROPIC_API_KEY` افتراضياً.
- ما يمرر طلب المستخدم عبر shell.
- يتجاوز حدث المخرجات الضخم ويكمل، مع سقف صلب يوقف الحالة غير الطبيعية.
- يحرر قفل التنفيذ عند `exit` أو `close` أو انتهاء الوقت.
- ينقّي الأسرار والمسارات المطلقة بصيغ Windows وUNC وPOSIX من الردود والأخطاء والسجلات بخوارزمية خطية لا تعبر نهاية السطر. داخل روابط `scheme://` يحجب مسارات Windows وUNC الخام والمشفّرة حتى طبقتين، ويحجب في قيم الاستعلام والجزء `#` جذور POSIX النظامية والجذور الملتبسة مثل `/home` و`/Users` عندما يحتوي المسار مقطعين إضافيين على الأقل؛ ويحفظ مسارات الويب العامة القصيرة مثل `/home/dashboard` و`/users/42`. وإذا بدأت كتلة مفتاح خاص ولم تصل علامة نهايتها بسبب بتر المخرج، يُحجب كل ما بعدها بدلاً من تسريب جسم المفتاح.
- يشفّر رمز البوت على القرص بـDPAPI بنطاق المستخدم الحالي، ويضيّق ACL لملف الإعداد.
- يحفظ كل جزء من الرسالة الخارجة قبل إرساله، ويسجل `message_id` للأجزاء المؤكدة، ويعيد فقط الجزء غير المؤكد بتراجع متدرج.
- يضع للصندوق سقف 500 عنصر وTTL مدته 7 أيام، ثم ينقل الأقدم أو المنتهي إلى dead-letter محدودة.
- يتحقق من بنية ملف الحالة، ويعزل التالف ويفشل مغلقاً بدل البدء بحالة فارغة. يحتفظ بنسخة `state.json.bak` ولا يستعيدها إلا بطلب صريح من النافذة.
- يتحقق من أن `update_id` عدد صحيح آمن وغير سالب قبل تنفيذ التحديث أو حفظه، ويرفض القيمة التالفة قبل أن تغيّر الإزاحة الدائمة.
- يدور سجل التشغيل عند 5 MiB أثناء بقاء العملية حية.
- الجسر لا يحدّث نفسه ولا يسحب كوداً. Claude Code المثبّت بالتنصيب الأصلي تبعية خارجية تتحدّث تلقائياً؛ النافذة تعرض المصدر والأثر وتطلب موافقة قبل تثبيتها.

## وش ما يقدر يضمنه؟

- ما يقدر يجعل خيار تجاوز الصلاحيات آمناً.
- ما يقدر يمنع Claude من تنفيذ طلب ضار إذا أنت سمحت له بالصلاحيات.
- زر الموافقة يؤكد قرارك للطلب المحدد، لكنه ما يجعل العمل الذي وافقت عليه آمناً بحد ذاته؛ اقرأ الوصف وارفض إذا ما فهمته.
- ما يراجع كل ملف أو رابط ترسله قبل ما يقرأه Claude.
- ما يعزل Claude داخل جهاز افتراضي أو صندوق حماية.
- DPAPI يحمي الرمز من حساب ويندوز آخر ومن قراءة الملف كنص، لكنه **لا** يحميه من برنامج خبيث يعمل بنفس حساب المستخدم.
- بصمة حزمة الجسر لا تغطي تحديثات Claude Code اللاحقة؛ Claude برنامج مستقل وله دورة تحديثه الخاصة.
- Telegram Bot API ما يوفر مفتاح idempotency للإرسال. لذلك الضمان at-least-once: قد يتكرر الجزء الذي قبله تيليجرام ثم ضاع الرد الشبكي، حتى مع تسجيل `message_id` لكل جزء مؤكد.
- بعد 7 أيام، أو عند تجاوز 500 ظرف pending (الظرف = جواب واحد وقد يكون عدة أجزاء) أو 1100 سجل إجمالاً، يوقف الصندوق محاولة الأقدم ويضع سجلها المختصر في dead-letter (بحد 50 سجلاً) حتى ما ينمو ملف الحالة بلا حد.
- بوابات Anthropic الخاصة وproxy/CA المؤسسية خارج نطاق الإصدار الأول؛ استخدم اتصال Claude الشخصي المباشر المدعوم.
- **حدّ معروف:** حماية الظرف متعدد الأجزاء من الطرد تقيس عدد سجلاته الباقية لا عدده الأصلي. فلو بقي جزء واحد معلقاً من جواب طويل بعد تنظيف الأجزاء المسلّمة (بعد 24 ساعة)، **وتزامن ذلك مع بلوغ سعة الصندوق حدَّها الكامل**، فقد يُنقل ذلك الجزء إلى dead-letter. حالة نادرة لا تُعطّل التشغيل، ومسجَّلة للمعالجة في إصدار لاحق.
- **حدّ معروف في التنقيح:** لا يمكن التمييز قطعياً بين مسار محلي ومسار ويب إذا كانا متطابقين نصياً. لذلك يقدّم المنقّي الخصوصية عند وجود بنية محلية قوية، وقد يحجب بقية سطر نثري بعد مسار Windows يحتوي مسافات. كما أن نصاً ملتصقاً بـ`scheme://` قد يُفسَّر كاسم scheme صالح؛ هذا لا يمنع حجب مسار محلي صريح داخل الرابط.

## الإبلاغ عن ثغرة

لا تنشر توكنات أو سجلات خاصة في بلاغ عام. افتح بلاغاً مختصراً بلا أسرار، واشرح:

- إصدار Node.js وويندوز.
- الخطوات اللي سببت المشكلة.
- النتيجة المتوقعة والنتيجة الفعلية.
- هل كنت على الوضع الآمن أو «أشتغل بحرية».

إذا كان البلاغ نفسه يحتوي سراً، ألغِ السر أولاً ثم أنشئ بديلاً جديداً قبل أي مشاركة.
